# HIRA: Hierarchical Range-Searching Attention
Coming soon...

## Updates
- [TBD] First release

## Project Organization
- `benchmark_area`: here we run experiments and compare smaller units of code.
- `tests`: the tests are here, to run: `pytest tests/* -vv`.
- `index`: the main range searching index is implemented here.

## Installation

```bash
cd hira
pip install -e .
```